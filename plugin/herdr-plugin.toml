id = "drover.ntfy-push"
name = "Drover ntfy push"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Push a phone notification via ntfy when an agent needs attention."
platforms = ["linux", "macos"]

[[events]]
on = "pane.agent_status_changed"
command = ["sh", "notify.sh"]
